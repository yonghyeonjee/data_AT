# 비밀값 — 이 저장소는 db.samsungat.co.kr 로 공개됩니다. 절대 올리지 마세요.
_secrets.local.md
.env
.env.local
*.key

# 테스트·작업 파일 (웹 서버에 있을 이유가 없음)
ptest/
node_modules/
*.log
.DS_Store

# Apps Script 원본은 웹에 올리지 않습니다 (tools/gas 안에만 두거나 아예 저장소 밖)
/*.gs
/*.php
/*.mjs
/*.yml

# 잘못 올라가기 쉬운 것
/mvp_*.sql
"* (*)"
